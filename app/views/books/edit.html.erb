<div class="row">
<%= render 'book', user: @book_a.user, book: @book %>

  <div class="offst-md-1">
  <div class="col-md-8">

<h1>Editing Book</h1>
  <% if @book_a.errors.any? %>
    <%= @book_a.errors.count %>error prohibited this book from being saved
      <% @book_a.errors.full_messages.each do |message| %>
         <li><%= message %></li>
      <% end %>
  <% end %>

<%= form_with model: @book_a do |f| %>

  <h4>Title</h4>

  <%= f.text_field :title %>

  <h4>Body</h4>

  <%= f.text_area :body %>

  <%= f.submit 'Update Book'%>

<% end %>
<%= link_to "Show", book_path(@book_a.id) %>
|
<%= link_to "Back", books_path %>
</div>
</div>
</div>