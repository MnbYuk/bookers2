<%= render 'book', book: @book %>

    <div class="col-md-8">
<table>
    <thead>
        <th>Title</th>
        <th>Book</th>
    </thead>

    <tbody>
        <% @books.each do |book| %>
            <tr>
                <td><span><%= link_to book.title, book_path(book.id)%></span></td>
                <td><span><%= book.body %></span></td>
            </tr>
        <% end %>

    </tbody>

</table>
    </div>
  </div>
</div>