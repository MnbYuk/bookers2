<%= render 'book', book: @book %>
<h1>Book detail</h1>
<p>タイトル<%= @book.title %></p>
<p>説明　<%= @book.body %></p>
<p>えぢっと</p>
<p>
  <% if @book.user == current_user %>
    <%= link_to "削除", book_path(@book), method: :delete %>
  <% end %>
</p>